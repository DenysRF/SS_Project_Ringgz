package GameObjects;

public class StartingBase extends Base {

    /*
     Starting base is much like a normal base
     but it can be identified as instanceof StartingBase
     and the owner is null
      */
    public StartingBase() {
        super(null);
    }
}
